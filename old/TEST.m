% INPUT
% 
% [domain, boundary_upper_particles_number_particles] = function_boundary_initialization(boundary)
% 
% domain = function_boundary_outflow(domain_new, boundary.general.channel_end);

for m = 10:-1:1
    m
end